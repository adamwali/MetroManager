# Types

Shared TypeScript type definitions. Defined in Phase 1.1 (GameState and its sub-types).

Boundary rule: types cannot depend on engine, state, ui, or app modules.
They describe shapes; they don't implement behavior.
